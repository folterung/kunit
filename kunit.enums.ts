export enum kunitEnums {
  mock,
  fixture,
  instance,
  beforeAll,
  beforeAllAsync,
  beforeEach,
  beforeEachAsync,
  test,
  testAsync,
  afterEach,
  afterEachAsync,
  afterAll,
  afterAllAsync
}
